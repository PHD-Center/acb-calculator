name = "acb-voice"
main = "worker.js"
compatibility_date = "2024-11-01"

# Cloudflare Workers AI binding — gives the Worker access to env.AI.run(...)
[ai]
binding = "AI"

# Shared "voice learning" dictionary — pooled cross-user sound→drug corrections.
[[kv_namespaces]]
binding = "VOICE_KV"
id = "918b391e655a45b1b16c0588670473af"
